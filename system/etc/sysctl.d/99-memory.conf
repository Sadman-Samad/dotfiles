# Desktop memory tuning — prevent hangs on RAM pressure
# Only swap under real pressure (default 60 is too aggressive for desktops)
vm.swappiness=10
# Reclaim inode/dentry cache more aggressively
vm.vfs_cache_pressure=50
