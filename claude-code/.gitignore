# Machine-specific configuration with user stats, tips, and cached data
# Contains MCP server configurations but also non-shareable data
# Symlinked for convenience but not committed to git
.claude.json
