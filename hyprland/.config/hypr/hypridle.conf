# hypridle — disabled (never sleep / never lock / never screen-off)
# Original listeners commented out below for reference.

general {
    # No lock_cmd needed — nothing triggers a lock.
}

# listener {
#     timeout = 180 # 3mins
#     on-timeout = loginctl lock-session
# }
#
# listener {
#     timeout = 240 # 4mins
#     on-timeout = hyprctl dispatch dpms off
#     on-resume = hyprctl dispatch dpms on
# }
#
# listener {
#     timeout = 540 # 9mins
#     on-timeout = $suspend_cmd
# }
